# 阶段二：刷题功能增强 - 详细设计

## 📋 设计目标

完善刷题体验，增加以下核心功能：
1. **AI 实时问答辅助** - 用户可以随时向 AI 提问
2. **代码版本回溯** - 保存多个代码版本，支持恢复
3. **测试结果优化** - 显示失败原因和 AI 建议
4. **答题记录增强** - 记录答题时长、通过率等详细信息

## 🎯 核心功能设计

### 1. AI 实时问答辅助（AIAssistant）

**目标**：在刷题过程中，用户可以随时向 AI 提问，获得实时帮助。

#### 功能需求
- ✅ 用户输入问题，AI 实时回复
- ✅ 一键获取思路提示（不含具体代码）
- ✅ 知识点讲解
- ✅ 错误诊断和建议
- ✅ 历史对话记录

#### 类设计：AIAssistant

**文件**: `src/ai/AIAssistant.h/cpp`

```cpp
class AIAssistant : public QObject {
    Q_OBJECT
public:
    explicit AIAssistant(OllamaClient *aiClient, QObject *parent = nullptr);
    
    // 提问
    void askQuestion(const QString &question, const Question &currentQuestion);
    
    // 获取思路提示（不含代码）
    void getHint(const Question &currentQuestion);
    
    // 知识点讲解
    void explainConcept(const QString &concept);
    
    // 错误诊断
    void diagnoseError(const QString &code, const QString &errorMessage);
    
    // 获取历史对话
    QVector<ChatMessage> getChatHistory() const;
    
    // 清空历史
    void clearHistory();
    
signals:
    void responseReady(const QString &response);
    void error(const QString &errorMessage);
    
private:
    QString buildPrompt(const QString &userInput, const Question &question);
    QString buildHintPrompt(const Question &question);
    
    OllamaClient *m_aiClient;
    QVector<ChatMessage> m_chatHistory;
};

struct ChatMessage {
    QString role;      // "user" or "assistant"
    QString content;
    QDateTime timestamp;
};
```

#### UI 组件：AI 辅助面板

在刷题页面添加 AI 辅助面板（可折叠）：

```
┌─────────────────────────────────────┐
│ 🤖 AI 助手                          │
├─────────────────────────────────────┤
│ [💡 获取思路提示]  [📚 知识点讲解]  │
├─────────────────────────────────────┤
│ 对话历史：                          │
│ 👤 用户: 这道题用什么算法？         │
│ 🤖 AI: 这道题可以使用哈希表...      │
│                                     │
│ 👤 用户: 时间复杂度是多少？         │
│ 🤖 AI: 时间复杂度是 O(n)...        │
├─────────────────────────────────────┤
│ 输入问题：                          │
│ [_____________________________]     │
│                        [发送]       │
└─────────────────────────────────────┘
```

### 2. 代码版本回溯（CodeVersionManager）

**目标**：自动保存代码的多个版本，用户可以查看和恢复历史版本。

#### 功能需求
- ✅ 自动保存代码版本（带时间戳）
- ✅ 显示版本列表（时间、代码行数、是否通过测试）
- ✅ 对比两个版本的差异
- ✅ 恢复到指定版本
- ✅ 删除旧版本（保留最近 10 个）

#### 类设计：CodeVersionManager

**文件**: `src/core/CodeVersionManager.h/cpp`

```cpp
struct CodeVersion {
    QString versionId;        // 唯一ID
    QString questionId;       // 题目ID
    QString code;             // 代码内容
    QDateTime timestamp;      // 保存时间
    int lineCount;            // 代码行数
    bool testPassed;          // 是否通过测试
    QString testResult;       // 测试结果摘要
};

class CodeVersionManager : public QObject {
    Q_OBJECT
public:
    explicit CodeVersionManager(QObject *parent = nullptr);
    
    // 保存新版本
    QString saveVersion(const QString &questionId, const QString &code, 
                       bool testPassed = false, const QString &testResult = "");
    
    // 获取版本列表
    QVector<CodeVersion> getVersions(const QString &questionId) const;
    
    // 获取指定版本
    CodeVersion getVersion(const QString &versionId) const;
    
    // 恢复到指定版本
    bool restoreVersion(const QString &versionId);
    
    // 对比两个版本
    QString compareVersions(const QString &versionId1, const QString &versionId2) const;
    
    // 删除旧版本（保留最近N个）
    void cleanOldVersions(const QString &questionId, int keepCount = 10);
    
signals:
    void versionSaved(const QString &versionId);
    void versionRestored(const QString &versionId);
    
private:
    QString getVersionFilePath(const QString &questionId, const QString &versionId) const;
    QString getVersionsDir(const QString &questionId) const;
};
```

#### UI 组件：版本历史对话框

```
┌─────────────────────────────────────────────────┐
│ 📜 代码版本历史 - 两数之和                      │
├─────────────────────────────────────────────────┤
│ 版本列表：                                      │
│                                                 │
│ ✅ 2024-12-02 15:30:45  (45 行) [通过 5/5]     │
│ ❌ 2024-12-02 15:25:12  (42 行) [通过 3/5]     │
│ ❌ 2024-12-02 15:20:33  (38 行) [通过 2/5]     │
│ ⏳ 2024-12-02 15:15:00  (35 行) [未测试]       │
│                                                 │
├─────────────────────────────────────────────────┤
│ 操作：                                          │
│ [查看代码] [恢复此版本] [对比版本] [删除]      │
└─────────────────────────────────────────────────┘
```

### 3. 测试结果优化（EnhancedTestResult）

**目标**：提供更详细的测试结果，包括失败原因和 AI 建议。

#### 功能需求
- ✅ 显示详细的失败原因（答案错误/运行超时/内存超限/编译错误）
- ✅ AI 分析失败原因，给出修改建议
- ✅ 标注测试数据来源（原始/AI 补充）
- ✅ 显示每个测试用例的描述

#### 数据结构增强

**修改**: `src/core/CompilerRunner.h`

```cpp
enum class TestFailureReason {
    None,              // 通过
    WrongAnswer,       // 答案错误
    RuntimeError,      // 运行时错误
    TimeLimitExceeded, // 超时
    MemoryLimitExceeded, // 内存超限
    CompileError       // 编译错误
};

struct TestResult {
    bool passed;
    QString input;
    QString expectedOutput;
    QString actualOutput;
    QString description;           // 测试用例描述
    TestFailureReason failureReason; // 失败原因
    QString errorMessage;          // 错误信息
    int executionTime;             // 执行时间（毫秒）
    bool isAIGenerated;            // 是否 AI 生成的测试数据
};
```

#### AI 建议生成

```cpp
class TestResultAnalyzer {
public:
    // 分析测试结果，生成 AI 建议
    QString analyzeFailures(const QVector<TestResult> &results, 
                           const QString &code,
                           OllamaClient *aiClient);
    
private:
    QString buildAnalysisPrompt(const QVector<TestResult> &failures, 
                               const QString &code);
};
```

#### UI 显示优化

```
┌─────────────────────────────────────────────────┐
│ ❌ Wrong Answer - 通过 3/5 测试用例             │
├─────────────────────────────────────────────────┤
│ ✅ 测试用例 1/5 - 基本测试 [原始]              │
│    输入：[2,7,11,15], 9                         │
│    期望：[0,1]                                  │
│    实际：[0,1]                                  │
│    ⏱️ 执行时间：2ms                             │
│                                                 │
│ ✅ 测试用例 2/5 - 不同位置 [原始]              │
│    输入：[3,2,4], 6                             │
│    期望：[1,2]                                  │
│    实际：[1,2]                                  │
│    ⏱️ 执行时间：1ms                             │
│                                                 │
│ ❌ 测试用例 3/5 - 边界条件 [AI补充]            │
│    输入：[], 0                                  │
│    期望：[]                                     │
│    实际：null                                   │
│    ❗ 失败原因：答案错误                        │
│    ⏱️ 执行时间：0ms                             │
│                                                 │
│ 💡 AI 建议：                                    │
│ 检测到空数组处理问题。建议：                   │
│ 1. 在函数开始添加空数组检查                    │
│ 2. 返回空数组而不是 null                       │
│ 3. 参考代码片段：                              │
│    if (nums.empty()) return {};                │
└─────────────────────────────────────────────────┘
```

### 4. 答题记录增强（EnhancedAnswerRecord）

**目标**：记录更详细的答题信息，用于统计和分析。

#### 数据结构

**文件**: `src/core/AnswerRecord.h`

```cpp
struct AnswerRecord {
    QString recordId;          // 记录ID
    QString questionId;        // 题目ID
    QString questionTitle;     // 题目标题
    Difficulty difficulty;     // 难度
    QStringList tags;          // 标签
    
    // 答题状态
    QString status;            // 未开始/进行中/已完成/已掌握
    int attemptCount;          // 尝试次数
    int passedCount;           // 通过次数
    double passRate;           // 通过率
    
    // 时间统计
    QDateTime firstAttempt;    // 首次尝试时间
    QDateTime lastAttempt;     // 最后尝试时间
    int totalTimeSpent;        // 总用时（秒）
    int avgTimePerAttempt;     // 平均用时
    
    // 测试结果
    int totalTestCases;        // 总测试用例数
    int passedTestCases;       // 通过的测试用例数
    QString lastTestResult;    // 最后一次测试结果
    
    // 代码信息
    QString lastCodeVersion;   // 最后代码版本ID
    int codeLineCount;         // 代码行数
    
    // AI 辅助
    int aiHintUsed;            // 使用 AI 提示次数
    int aiQuestionAsked;       // 向 AI 提问次数
    
    QString toJson() const;
    static AnswerRecord fromJson(const QString &json);
};

class AnswerRecordManager : public QObject {
    Q_OBJECT
public:
    // 记录答题开始
    void recordAttemptStart(const QString &questionId);
    
    // 记录测试结果
    void recordTestResult(const QString &questionId, 
                         const QVector<TestResult> &results,
                         int timeSpent);
    
    // 记录 AI 使用
    void recordAIUsage(const QString &questionId, bool isHint);
    
    // 获取答题记录
    AnswerRecord getRecord(const QString &questionId) const;
    
    // 获取统计信息
    QMap<QString, int> getStatistics() const;
};
```

## 📊 实现优先级

### 高优先级（必须实现）
1. ✅ **代码版本回溯** - 防止代码丢失，用户体验关键
2. ✅ **测试结果优化** - 显示失败原因，帮助调试
3. ✅ **答题记录增强** - 统计分析基础

### 中优先级（建议实现）
4. ✅ **AI 实时问答** - 提升学习体验

### 低优先级（可选）
5. ⏳ 版本对比功能
6. ⏳ AI 建议生成（需要额外 AI 调用）

## 🔧 实现步骤

### Step 1: 创建 CodeVersionManager ⭐⭐⭐
- 实现代码版本保存和加载
- 实现版本列表管理
- 实现版本恢复功能

### Step 2: 增强 CompilerRunner ⭐⭐⭐
- 添加失败原因枚举
- 扩展 TestResult 结构
- 实现执行时间统计

### Step 3: 创建 AIAssistant ⭐⭐
- 实现问答功能
- 实现思路提示
- 实现对话历史

### Step 4: 创建 AnswerRecordManager ⭐⭐
- 实现答题记录保存
- 实现统计功能

### Step 5: UI 集成 ⭐⭐⭐
- 在 PracticeWidget 中添加 AI 辅助面板
- 创建版本历史对话框
- 优化测试结果显示

## 🎯 验收标准

- [ ] 代码自动保存多个版本
- [ ] 可以查看和恢复历史版本
- [ ] 测试结果显示失败原因
- [ ] 测试结果显示执行时间
- [ ] AI 助手可以回答问题
- [ ] AI 助手可以提供思路提示
- [ ] 答题记录包含详细统计信息
- [ ] 编译通过，无错误

## 📝 使用场景

### 场景 1：代码版本回溯
```
1. 用户编写代码，自动保存版本 v1
2. 运行测试，通过 2/5，保存版本 v2（标记测试结果）
3. 修改代码，运行测试，通过 4/5，保存版本 v3
4. 修改代码，运行测试，通过 1/5（改坏了！）
5. 用户打开版本历史，恢复到 v3
6. 继续修改...
```

### 场景 2：AI 实时辅助
```
1. 用户看到题目，不知道用什么算法
2. 点击"获取思路提示"
3. AI 回复："这道题可以使用哈希表，时间复杂度 O(n)..."
4. 用户编写代码，遇到边界条件问题
5. 输入问题："空数组怎么处理？"
6. AI 回复："对于空数组，应该返回空数组..."
```

### 场景 3：测试结果分析
```
1. 用户运行测试，通过 3/5
2. 系统显示：
   - ✅ 测试用例 1: 基本测试 [通过] 2ms
   - ✅ 测试用例 2: 不同位置 [通过] 1ms
   - ❌ 测试用例 3: 边界条件 [失败] 答案错误
   - ❌ 测试用例 4: 空数组 [失败] 运行时错误
   - ✅ 测试用例 5: 大数组 [通过] 5ms
3. AI 建议："检测到空数组和边界条件处理问题..."
```

---

**设计完成，准备实施！**

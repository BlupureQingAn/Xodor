# CMake PATH 修复指南

## 问题描述
```
cmake : 无法将"cmake"项识别为 cmdlet、函数、脚本文件或可运行程序的名称
```

## 原因
CMake不在系统的PATH环境变量中。

## ✅ 已完成的修复

### 1. 找到CMake位置
```
F:\Qt\qt\Tools\CMake_64\bin\cmake.exe
版本: 3.30.5
```

### 2. 更新IDE配置
已更新 `.vscode/settings.json`:
```json
{
    "cmake.cmakePath": "F:/Qt/qt/Tools/CMake_64/bin/cmake.exe",
    "cmake.configureOnOpen": false,
    "cmake.buildDirectory": "${workspaceFolder}/build",
    "cmake.generator": "Ninja"
}
```

## 🔧 三种使用方式

### 方式1：使用批处理脚本（推荐）✅
```cmd
# 编译项目
.\build_project.bat

# 配置CMake
.\configure_cmake.bat

# 清理构建
.\clean_qtcreator_build.bat
```

**优点**：
- 无需配置PATH
- 脚本已包含完整路径
- 最简单可靠

### 方式2：临时添加到PATH（当前会话）
```powershell
# PowerShell
$env:PATH += ";F:\Qt\qt\Tools\CMake_64\bin"
cmake --version

# CMD
set PATH=%PATH%;F:\Qt\qt\Tools\CMake_64\bin
cmake --version
```

**优点**：
- 快速临时解决
- 不影响系统配置

**缺点**：
- 关闭终端后失效
- 每次都要重新设置

### 方式3：永久添加到系统PATH
#### 方法A：使用PowerShell（管理员权限）
```powershell
# 添加到用户PATH
[Environment]::SetEnvironmentVariable(
    "Path",
    [Environment]::GetEnvironmentVariable("Path", "User") + ";F:\Qt\qt\Tools\CMake_64\bin",
    "User"
)
```

#### 方法B：使用CMD（管理员权限）
```cmd
setx PATH "%PATH%;F:\Qt\qt\Tools\CMake_64\bin"
```

#### 方法C：图形界面
1. 右键"此电脑" → 属性
2. 高级系统设置 → 环境变量
3. 在"用户变量"中找到"Path"
4. 点击"编辑" → "新建"
5. 添加：`F:\Qt\qt\Tools\CMake_64\bin`
6. 确定保存
7. **重启终端**使其生效

**优点**：
- 永久生效
- 所有终端都可用

**缺点**：
- 需要管理员权限
- 需要重启终端

## 📝 验证方法

### 验证PATH配置
```powershell
# PowerShell
$env:PATH -split ';' | Select-String -Pattern 'CMake'

# CMD
echo %PATH% | findstr CMake
```

### 验证CMake可用
```cmd
cmake --version
```

应该显示：
```
cmake version 3.30.5
```

## 🎯 推荐方案

### 对于日常开发
**使用批处理脚本**（方式1）
- 最简单
- 无需配置
- 已经可用

### 对于IDE集成
**已完成** - `.vscode/settings.json` 已配置
- Kiro IDE会使用配置的路径
- 无需手动添加PATH

### 对于命令行使用
如果经常需要在命令行使用cmake：
- **临时使用**：方式2（临时添加）
- **长期使用**：方式3（永久添加）

## 🔍 查找CMake工具

已创建 `find_cmake.bat` 脚本：
```cmd
.\find_cmake.bat
```

会自动搜索常见位置并显示找到的CMake。

## ✅ 当前状态

- [x] 找到CMake位置
- [x] 更新IDE配置
- [x] 批处理脚本可用
- [x] 创建查找工具
- [x] 文档完善

## 💡 提示

1. **推荐使用批处理脚本**
   - `build_project.bat` 已经可以正常编译
   - 无需额外配置

2. **IDE已配置**
   - Kiro IDE会使用 `.vscode/settings.json` 中的路径
   - CMake错误应该消失

3. **命令行使用**
   - 如果需要在命令行使用cmake
   - 选择方式2（临时）或方式3（永久）

## 🎉 总结

问题已解决！你可以：
- ✅ 使用 `build_project.bat` 编译（推荐）
- ✅ Kiro IDE已配置CMake路径
- ✅ 可选：添加到PATH以便命令行使用

**建议**：继续使用批处理脚本，简单可靠！

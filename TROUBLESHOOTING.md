# 故障排查 - 代码保存和语法检查

## 🔍 当前问题

1. **代码没有保存** - 切换题目后代码丢失
2. **没有调试日志输出** - 无法看到保存和检查过程
3. **错误列表不工作** - 语法错误没有显示

## 📋 排查步骤

### 步骤1：确认目录存在 ✅
```bash
# 已创建目录
data/user_answers/
```

### 步骤2：检查调试日志

**预期的日志输出：**
```
[CodeEditor] Syntax checker initialized successfully
[CodeEditor] Question ID set to: Q001
[AutoSaver] Question ID set to: Q001
[CodeEditor] Text changed, code length: 50
[AutoSaver] Saved code to: data/user_answers/Q001.json length: 50
[CodeEditor] Triggering syntax check...
[SyntaxChecker] Performing syntax check with compiler: g++
```

**如果没有看到任何日志：**
- 可能是在Release模式运行（qDebug被禁用）
- 或者控制台被重定向了

### 步骤3：手动测试保存功能

1. **启动程序**
2. **导入题库或选择题目**
3. **切换到刷题模式**（点击"✏️ 刷题模式"）
4. **在代码编辑器中输入代码**
5. **等待1秒**
6. **检查data/user_answers目录**

### 步骤4：验证文件是否创建

运行以下命令检查：
```bash
dir data\user_answers
```

应该看到类似的文件：
```
Q001.json
Q002.json
...
```

## 🐛 可能的原因

### 原因1：没有选择题目
**症状：** 看到警告 `[AutoSaver] WARNING: questionId is empty`

**解决：**
1. 确保已导入题库
2. 在题库列表中选择一个题目
3. 切换到刷题模式

### 原因2：在题库列表模式
**症状：** 看不到代码编辑器

**解决：**
1. 点击工具栏的"✏️ 刷题模式"按钮
2. 或按快捷键 Ctrl+E

### 原因3：文本变化事件未触发
**症状：** 没有看到 `[CodeEditor] Text changed` 日志

**解决：**
1. 确认在代码编辑器中输入了内容
2. 检查编辑器是否获得了焦点
3. 尝试输入多个字符

### 原因4：编译器未安装
**症状：** 看到 `[SyntaxChecker] Process did not exit normally`

**解决：**
1. 安装MinGW或GCC
2. 将编译器路径添加到系统PATH
3. 在设置中配置编译器路径

## 🔧 临时解决方案

### 方案1：强制保存
在MainWindow中添加一个"保存"按钮，手动触发保存：

```cpp
// 在工具栏添加保存按钮
QAction *saveAction = toolBar->addAction("💾 保存代码");
connect(saveAction, &QAction::triggered, [this]() {
    m_codeEditor->autoSaver()->performSave();
});
```

### 方案2：禁用延迟保存
修改AutoSaver，立即保存而不延迟：

```cpp
void AutoSaver::triggerSave()
{
    // 立即保存，不使用定时器
    performSave();
}
```

### 方案3：使用Release模式的日志
如果在Release模式下运行，qDebug不会输出。可以改用qWarning或qInfo：

```cpp
qWarning() << "[CodeEditor] Text changed...";  // 总是输出
```

## 📝 测试清单

请按以下步骤测试：

- [ ] 1. 启动程序，看到主界面
- [ ] 2. 点击"🤖 AI导入题库"导入题目
- [ ] 3. 在题库列表中看到题目
- [ ] 4. 双击一个题目或点击"✏️ 刷题模式"
- [ ] 5. 看到代码编辑器和题目描述
- [ ] 6. 在代码编辑器中输入代码
- [ ] 7. 等待1秒后，检查控制台日志
- [ ] 8. 检查data/user_answers目录是否有JSON文件
- [ ] 9. 切换到另一个题目
- [ ] 10. 再切换回来，检查代码是否保存

## 🎯 快速诊断命令

### 检查目录和文件
```bash
# Windows
dir data\user_answers
type data\user_answers\*.json

# 查看最近修改的文件
dir data\user_answers /O-D
```

### 检查程序是否在运行
```bash
tasklist | findstr CodePracticeSystem
```

### 强制重新编译
```bash
taskkill /F /IM CodePracticeSystem.exe
.\build_utf8.bat
```

## 💡 建议

1. **先导入一个简单的题库测试**
2. **在Debug模式下运行程序**
3. **保持控制台窗口可见**
4. **每次输入后等待1秒观察日志**
5. **检查data/user_answers目录的文件**

## 📞 如果问题仍然存在

请提供以下信息：
1. 控制台的完整输出
2. data/user_answers目录的内容
3. 是否看到代码编辑器
4. 是否选择了题目
5. 是否在刷题模式

---

**下一步：** 运行程序，按照测试清单操作，并告诉我在哪一步出现了问题。

# 题库管理功能重复对比表

## 一、QuestionListWidget 是什么？

**QuestionListWidget** 是一个**孤立的、未使用的文件**：

### 基本信息
- **文件位置**：`src/ui/QuestionListWidget.cpp`
- **头文件**：❌ 不存在（没有 .h 文件）
- **编译状态**：❌ 未在 CMakeLists.txt 中
- **使用状态**：❌ 没有任何代码引用它
- **结论**：这是一个**废弃的实验性代码**，应该删除

### 它的功能（如果被使用的话）

QuestionListWidget 实现了与 QuestionBankPanel **完全相同**的双模式设计：

#### 模式1：题目列表模式
- 显示题目列表
- 搜索题目
- 难度筛选
- 显示题目状态图标
- 选择和删除题目

#### 模式2：题库列表模式
- 扫描 `data/基础题库/` 目录
- 显示题库列表
- 加载题库
- 删除题库（直接删除文件夹）
- 重命名题库（直接重命名文件夹）

### 为什么存在？

可能的原因：
1. 早期开发时的实验性代码
2. 重构时忘记删除的旧版本
3. 从 QuestionBankPanel 复制出来准备修改，但最终没用上

---

## 二、功能重复对比表

### 2.1 题库管理对话框 vs 题库面板（题库模式）

| 功能 | QuestionBankManagerDialog | QuestionBankPanel (题库模式) | QuestionListWidget (题库模式) | 重复？ |
|------|---------------------------|------------------------------|-------------------------------|--------|
| **显示题库列表** | ✅ | ✅ | ✅ | ❌❌❌ 三重重复 |
| **切换题库** | ✅ | ✅ | ✅ | ❌❌❌ 三重重复 |
| **删除题库** | ✅ | ✅ | ✅ | ❌❌❌ 三重重复 |
| **重命名题库** | ✅ | ✅ | ✅ | ❌❌❌ 三重重复 |
| **查看题库详情** | ✅ 详细统计 | ❌ | ❌ | - |
| **刷新题库** | ✅ | ✅ | ✅ | ❌❌❌ 三重重复 |
| **导入新题库** | ✅ | ❌ | ❌ | - |
| **导出路径** | ✅ | ❌ | ❌ | - |

### 2.2 数据源对比

| 组件 | 数据源 | 获取方式 | 问题 |
|------|--------|---------|------|
| **QuestionBankManagerDialog** | QuestionBankManager | `QuestionBankManager::instance().getAllBanks()` | ✅ 正确 |
| **QuestionBankPanel (题库模式)** | 文件系统 | 直接扫描 `data/基础题库/` | ❌ 不统一 |
| **QuestionListWidget (题库模式)** | 文件系统 | 直接扫描 `data/基础题库/` | ❌ 不统一 |

### 2.3 题库操作实现对比

#### 删除题库

**QuestionBankManagerDialog**：
```cpp
bool QuestionBankManager::deleteQuestionBank(const QString &bankId)
{
    // 1. 从管理器中移除注册信息
    m_banks.removeAt(index);
    
    // 2. 保存配置
    save();
    
    // 3. 发出信号
    emit bankListChanged();
    
    // 注意：不删除实际文件，只是注销
}
```
✅ **优点**：
- 使用管理器API
- 保持数据一致性
- 不破坏文件

**QuestionBankPanel (题库模式)**：
```cpp
void QuestionBankPanel::onDeleteBank()
{
    // 1. 直接删除基础题库文件夹
    QDir baseDir(bankPath);
    baseDir.removeRecursively();
    
    // 2. 直接删除原始题库文件夹
    QString originalPath = QString("data/原始题库/%1").arg(bankName);
    QDir originalDir(originalPath);
    originalDir.removeRecursively();
    
    // 3. 清除会话状态
    SessionManager::instance().clearSession();
    
    // 注意：直接操作文件系统，不通知管理器
}
```
❌ **问题**：
- 绕过管理器
- QuestionBankManager 中的注册信息不会更新
- 可能导致数据不一致

**QuestionListWidget (题库模式)**：
```cpp
// 与 QuestionBankPanel 完全相同的实现
```
❌ **问题**：代码完全重复

---

#### 重命名题库

**QuestionBankManagerDialog**：
```cpp
bool QuestionBankManager::renameQuestionBank(const QString &bankId, const QString &newName)
{
    // 1. 更新管理器中的名称
    bank.name = newName;
    
    // 2. 保存配置
    save();
    
    // 3. 发出信号
    emit bankListChanged();
    
    // 注意：只改名称，不移动文件
}
```
✅ **优点**：
- 使用管理器API
- 保持元数据一致

**QuestionBankPanel (题库模式)**：
```cpp
void QuestionBankPanel::onRenameBank()
{
    // 1. 直接重命名基础题库文件夹
    QDir().rename(bankPath, newPath);
    
    // 2. 直接重命名原始题库文件夹
    QString oldOriginalPath = QString("data/原始题库/%1").arg(oldName);
    QString newOriginalPath = QString("data/原始题库/%1").arg(newName);
    QDir().rename(oldOriginalPath, newOriginalPath);
    
    // 注意：直接操作文件系统，不通知管理器
}
```
❌ **问题**：
- 绕过管理器
- QuestionBankManager 中的路径信息不会更新
- 导致题库无法找到

---

#### 加载题库列表

**QuestionBankManagerDialog**：
```cpp
void QuestionBankManagerDialog::refreshBankList()
{
    // 从管理器获取所有已注册的题库
    QVector<QuestionBankInfo> banks = QuestionBankManager::instance().getAllBanks();
    
    for (QuestionBankInfo info : banks) {
        // 显示题库信息：名称、题目数、完成度、导入时间等
        QString text = formatBankInfo(info);
        // ...
    }
}
```
✅ **优点**：
- 使用管理器API
- 显示完整的元数据
- 支持完成度统计

**QuestionBankPanel (题库模式)**：
```cpp
void QuestionBankPanel::updateBankList()
{
    // 直接扫描文件系统
    QDir baseDir("data/基础题库");
    QStringList banks = baseDir.entryList(QDir::Dirs | QDir::NoDotAndDotDot);
    
    for (const QString &bankName : banks) {
        // 只显示名称和题目数
        QString info = getBankInfo(bankPath);
        // ...
    }
}
```
❌ **问题**：
- 绕过管理器
- 无法显示元数据（导入时间、完成度等）
- 可能显示未注册的题库

---

## 三、具体重复的功能

### 🔴 完全重复的功能（三个组件都有）

1. **显示题库列表**
   - QuestionBankManagerDialog ✅ 使用管理器
   - QuestionBankPanel ❌ 扫描文件系统
   - QuestionListWidget ❌ 扫描文件系统

2. **切换/加载题库**
   - QuestionBankManagerDialog ✅ 通过管理器切换
   - QuestionBankPanel ❌ 发出信号让 MainWindow 加载
   - QuestionListWidget ❌ 发出信号让 MainWindow 加载

3. **删除题库**
   - QuestionBankManagerDialog ✅ 注销题库（不删文件）
   - QuestionBankPanel ❌ 直接删除文件夹
   - QuestionListWidget ❌ 直接删除文件夹

4. **重命名题库**
   - QuestionBankManagerDialog ✅ 更新元数据
   - QuestionBankPanel ❌ 直接重命名文件夹
   - QuestionListWidget ❌ 直接重命名文件夹

5. **搜索题库**
   - QuestionBankManagerDialog ❌ 没有
   - QuestionBankPanel ✅ 有
   - QuestionListWidget ✅ 有

---

## 四、数据源不统一的问题

### 问题描述

**QuestionBankManager** 是题库的核心管理器，维护了题库的元数据：
- 题库ID
- 题库名称
- 存储路径
- 原始路径
- 题目数量
- 导入时间
- 最后访问时间
- AI解析标记

但是：
- ✅ **QuestionBankManagerDialog** 使用 QuestionBankManager
- ❌ **QuestionBankPanel** 直接扫描文件系统
- ❌ **QuestionListWidget** 直接扫描文件系统

### 导致的问题

1. **数据不一致**
   ```
   QuestionBankManager 中注册的题库：
   - CCF (data/基础题库/CCF)
   - 算法基础 (data/基础题库/算法基础)
   
   文件系统中实际存在的题库：
   - CCF
   - 算法基础
   - 测试题库（未注册）
   
   QuestionBankPanel 会显示"测试题库"，但 QuestionBankManagerDialog 不会
   ```

2. **操作不同步**
   ```
   用户在 QuestionBankPanel 中删除题库
   → 文件被删除
   → QuestionBankManager 中的注册信息仍然存在
   → QuestionBankManagerDialog 仍然显示这个题库
   → 点击切换时报错"路径不存在"
   ```

3. **元数据丢失**
   ```
   QuestionBankPanel 只能显示：
   - 题库名称
   - 题目数量
   
   无法显示：
   - 导入时间
   - 完成度
   - AI解析标记
   - 最后访问时间
   ```

---

## 五、统一数据源的方案

### 方案：所有组件都使用 QuestionBankManager

#### 修改 QuestionBankPanel（如果保留题库模式）

**修改前**：
```cpp
void QuestionBankPanel::updateBankList()
{
    // 直接扫描文件系统
    QDir baseDir("data/基础题库");
    QStringList banks = baseDir.entryList(QDir::Dirs | QDir::NoDotAndDotDot);
    // ...
}
```

**修改后**：
```cpp
void QuestionBankPanel::updateBankList()
{
    // 使用管理器
    QVector<QuestionBankInfo> banks = QuestionBankManager::instance().getAllBanks();
    
    for (const QuestionBankInfo &info : banks) {
        QString displayText = QString("📚 %1 (%2 道题目)")
            .arg(info.name)
            .arg(info.questionCount);
        
        QListWidgetItem *item = new QListWidgetItem(displayText);
        item->setData(Qt::UserRole, info.id);  // 存储题库ID而不是路径
        m_questionList->addItem(item);
    }
}
```

#### 修改删除操作

**修改前**：
```cpp
void QuestionBankPanel::onDeleteBank()
{
    // 直接删除文件
    QDir baseDir(bankPath);
    baseDir.removeRecursively();
}
```

**修改后**：
```cpp
void QuestionBankPanel::onDeleteBank()
{
    QString bankId = item->data(Qt::UserRole).toString();
    
    // 使用管理器API
    if (QuestionBankManager::instance().deleteQuestionBank(bankId)) {
        QMessageBox::information(this, "删除成功", "题库已注销");
        refreshBankList();
    }
}
```

#### 修改重命名操作

**修改前**：
```cpp
void QuestionBankPanel::onRenameBank()
{
    // 直接重命名文件夹
    QDir().rename(bankPath, newPath);
}
```

**修改后**：
```cpp
void QuestionBankPanel::onRenameBank()
{
    QString bankId = item->data(Qt::UserRole).toString();
    
    // 使用管理器API
    if (QuestionBankManager::instance().renameQuestionBank(bankId, newName)) {
        QMessageBox::information(this, "重命名成功", 
            QString("题库已重命名为【%1】").arg(newName));
        refreshBankList();
    }
}
```

---

## 六、推荐方案总结

### 立即执行

1. **删除 QuestionListWidget.cpp**
   - 原因：孤立文件，未被使用，功能完全重复
   - 风险：无
   - 收益：减少代码冗余

2. **简化 QuestionBankPanel**
   - 删除题库列表模式（PanelMode::QuestionBanks）
   - 只保留题目列表功能
   - 原因：题库管理功能与 QuestionBankManagerDialog 重复
   - 风险：低（题库模式可能很少使用）
   - 收益：职责清晰，易于维护

### 如果必须保留 QuestionBankPanel 的题库模式

3. **统一数据源**
   - 修改 `updateBankList()` 使用 QuestionBankManager
   - 修改 `onDeleteBank()` 使用管理器API
   - 修改 `onRenameBank()` 使用管理器API
   - 原因：避免数据不一致
   - 风险：中（需要测试）
   - 收益：数据一致性

---

## 七、对比总结表

| 方面 | QuestionBankManagerDialog | QuestionBankPanel (题库模式) | QuestionListWidget |
|------|---------------------------|------------------------------|-------------------|
| **状态** | ✅ 正常使用 | ⚠️ 功能重复 | ❌ 孤立文件 |
| **数据源** | ✅ QuestionBankManager | ❌ 文件系统 | ❌ 文件系统 |
| **功能完整性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **元数据支持** | ✅ 完整 | ❌ 缺失 | ❌ 缺失 |
| **数据一致性** | ✅ 保证 | ❌ 可能不一致 | ❌ 可能不一致 |
| **建议** | 保留 | 简化或统一数据源 | 删除 |

---

## 结论

1. **QuestionListWidget** 是一个废弃的孤立文件，应该**立即删除**
2. **QuestionBankPanel 的题库模式**与 QuestionBankManagerDialog **功能重复**，建议**删除题库模式**
3. **数据源不统一**导致数据不一致，应该**统一使用 QuestionBankManager**
4. **QuestionBankManagerDialog** 是唯一应该保留的题库管理界面
